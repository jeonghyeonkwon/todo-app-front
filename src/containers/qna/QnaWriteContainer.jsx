import React from 'react';
import CreateBoard from '../../components/main/CreateBoard';
const QnaWriteContainer = () => {
  return <CreateBoard type="qna"></CreateBoard>;
};

export default QnaWriteContainer;
